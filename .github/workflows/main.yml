name: example-basic
# This workflow represents a set of basic End-to-End tests
on:
  push:
    branches:
      - 'hemanth_devops'
  pull_request:
  workflow_dispatch:

jobs:

  my_job:
    runs-on: ubuntu-20.04
    steps:
      - name: testing
        run: echo "hello world"

    steps: 
      - name: clone the git
        run: git clone  https://github.com/ankit20000/DevOpsSRE-PROJECT.git